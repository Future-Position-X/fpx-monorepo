items-index:
  handler: app.handlers.geo_items.index
  events:
    - http:
        path: collections/{collection_uuid}/items
        method: get
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Get items belonging to a collection"
          description: "All accessable items belonging to collection"
          tags:
            - items
          queryParams:
            - ${file(serverless/documentation/queryParams.yml):limit}
            - ${file(serverless/documentation/queryParams.yml):offset}
            - ${file(serverless/documentation/queryParams.yml):format}
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):collection_uuid}
          methodResponses:
            - ${file(serverless/documentation/methodResponses.yml):formatDependent}
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}

items-index-by-name:
  handler: app.handlers.geo_items.index_by_name
  events:
    - http:
        path: collections/by_name/{collection_name}/items
        method: get
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Get items belonging to a collection"
          description: "All accessable items belonging to collection"
          tags:
            - items
          queryParams:
            - ${file(serverless/documentation/queryParams.yml):limit}
            - ${file(serverless/documentation/queryParams.yml):offset}
            - ${file(serverless/documentation/queryParams.yml):format}
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):collection_name}
          methodResponses:
            - ${file(serverless/documentation/methodResponses.yml):formatDependent}
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}

items-get:
  handler: app.handlers.geo_items.get
  events:
    - http:
        path: items/{item_uuid}
        method: get
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Get item as geojson FeatureCollection"
          description: "Get item as geojson FeatureCollection."
          tags:
            - items
          queryParams:
            - ${file(serverless/documentation/queryParams.yml):format}
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):item_uuid}
          methodResponses:
            - ${file(serverless/documentation/methodResponses.yml):formatDependent}
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}

items-delete:
  handler: app.handlers.geo_items.delete
  events:
    - http:
        path: /items/{item_uuid}
        method: delete
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Delete item"
          description: "Delete item"
          tags:
            - items
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):item_uuid}
          methodResponses:
            - statusCode: "204"
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}

items-delete-all:
  handler: app.handlers.geo_items.delete_items
  events:
    - http:
        path: /collections/{collection_uuid}/items
        method: delete
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Delete all items"
          description: "Delete all items"
          tags:
            - items
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):collection_uuid}
          methodResponses:
            - statusCode: "204"
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}

items-update:
  handler: app.handlers.geo_items.update
  events:
    - http:
        path: /items/{item_uuid}
        method: put
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Update item"
          description: "Update item"
          tags:
            - items
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):item_uuid}
          requestModels:
            "application/json": "UpdateItem"
          methodResponses:
            - statusCode: "204"
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}
items-create:
  handler: app.handlers.geo_items.create
  events:
    - http:
        path: collections/{collection_uuid}/items
        method: post
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Create item"
          description: "Create item"
          tags:
            - items
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):collection_uuid}
          requestModels:
            "application/json": "CreateItem"
          methodResponses:
            - ${file(serverless/documentation/methodResponses.yml):uuid}
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}

items-create-from-geojson:
  handler: app.handlers.geo_items.create_from_geojson
  events:
    - http:
        path: collections/{collection_uuid}/items/geojson
        method: post
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Remove all items in collection and add those in geojson FeatureCollection"
          description: "Remove all items in collection and add those in geojson FeatureCollection"
          tags:
            - items
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):collection_uuid}
          requestModels:
            "application/json": "FeatureCollection"
          methodResponses:
            - ${file(serverless/documentation/methodResponses.yml):uuids}
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}


items-add-from-geojson:
  handler: app.handlers.geo_items.add_from_geojson
  events:
    - http:
        path: collections/{collection_uuid}/items/geojson
        method: put
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Add items to collection from geojson FeatureCollection"
          description: "Add items to collection from geojson FeatureCollection"
          tags:
            - items
          pathParams:
            - ${file(serverless/documentation/pathParams.yml):collection_uuid}
          requestModels:
            "application/json": "FeatureCollection"
          methodResponses:
            - ${file(serverless/documentation/methodResponses.yml):uuids}
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}
            
items-update-from-geojson:
  handler: app.handlers.geo_items.update_from_geojson
  events:
    - http:
        path: items/geojson
        method: put
        cors: true
        authorizer: authorizer
        documentation:
          summary: "Update items from geojson FeatureCollection"
          description: "Update items from geojson FeatureCollection"
          tags:
            - items
          requestModels:
            "application/json": "FeatureCollection"
          methodResponses:
            - ${file(serverless/documentation/methodResponses.yml):uuids}
            - ${file(serverless/documentation/methodResponses.yml):error404}
            - ${file(serverless/documentation/methodResponses.yml):error500}

walking-paths-generate:
  handler: app.handlers.geo_items.generate_walking_paths
  events:
    - http:
        path: collections/{collection_uuid}/items/ai/generate/walkingpaths
        method: post
        cors: true
        authorizer: authorizer
        summary: Generate random walking paths using starting points from collection and polygons from another collection as environment."
        description: "Generate random walking paths using starting points from collection and polygons from another collection as environment."
        queryParams:
          - ${file(serverless/documentation/queryParams.yml):steps}
          - ${file(serverless/documentation/queryParams.yml):agents}
          - ${file(serverless/documentation/queryParams.yml):starting_points_collection_uuid}
          - ${file(serverless/documentation/queryParams.yml):environment_collection_uuid}
        pathParams:
          - ${file(serverless/documentation/pathParams.yml):collection_uuid}

sensor-data-sequence:
  handler: app.handlers.geo_items.prediction_for_sensor_item
  events:
    - http:
        path: items/{item_uuid}/ai/sequence
        method: get
        cors: true
        authorizer: authorizer
        summary: "Get true and predicted data for a GSC sensor, within a give date range."
        description: "Get true and predicted data for a GSC sensor, within a give date range. Predictions extend current time with 48 hours."
        queryParams:
          - ${file(serverless/documentation/queryParams.yml):startdate}
          - ${file(serverless/documentation/queryParams.yml):enddate}
        pathParams:
          - ${file(serverless/documentation/pathParams.yml):item_uuid}
